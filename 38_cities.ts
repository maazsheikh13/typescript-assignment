function describe_city(city : string,country:string= 'defualt country.'){
    console.log( `${city} is in ${country}`);
}
describe_city('karachi','pakistan');
describe_city('istanbul','turkry');

describe_city('paris',);