// write a progrAM THAT creates obj containing these items

interface person{
name : string,
age : number,
nationality : string,
student : boolean,

}


let person :person = {
    age:26,
name : 'maaz',
nationality : 'pakistan',
student : true,
} 
 console.log(person)
interface car {
name : string,
color : string,
automatic : boolean, 
}
 let car :car ={
name : 'city',
color : 'yellow',
automatic : true,
 }
 console.log(car)